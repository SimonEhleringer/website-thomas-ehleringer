import styled from 'styled-components'

export const WillkommenWrapper = styled.div`
  display: flex;
  justify-content: center;
  width: 100%;
  height: calc(100vh - 5rem);
  background-color: var(--color-gray-light-light);
`

export const WillkommenContainer = styled.div`
    width: 100%;
    display: flex;
    justify-content: center;
    max-width: 1920px;

`