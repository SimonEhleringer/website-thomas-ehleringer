import React from 'react';
import { SubSectionComponent } from './style';

const SubSection = ({ children }) => {
  return <SubSectionComponent>{children}</SubSectionComponent>;
};

export default SubSection;
